# Зависимости проекта (Python 3.9+)
#
# Декодер RLAD (geely_cluster/) и тесты — на чистой стандартной библиотеке,
# сторонних пакетов НЕ требуют. Пакеты ниже нужны только вспомогательным
# скриптам в tools/.

# Рендер/экспорт изображений (tools/, визуализация декодированных surface).
Pillow>=9.0

# Дизассемблер ARM Thumb — восстановление формата из библиотек Infineon
# (tools/, разовый анализ; для повседневного использования декодера не нужен).
capstone>=5.0

# Опционально: запуск тестов через pytest (можно и без него —
# `python tests/test_rlad_decoder.py`).
# pytest>=7.0
